name: chit_chat
description: "An AI chatbot that can handle files."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.3.3
  bloc: ^8.1.3
  flutter_bloc: ^8.1.4
  equatable: ^2.0.3
  hydrated_bloc: ^9.1.4

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
